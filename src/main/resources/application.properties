#  Configuración de la base de datos

spring.datasource.url=jdbc:mysql://localhost:3306/proyecto_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=springuser
spring.datasource.password=18614410camilo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA / Hibernate

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configuración del servidor

server.port=8080

# Configuración JWT

# Clave secreta para firmar los tokens
jwt.secret=18614410camilo

# Tiempo de expiración de los tokens en milisegundos (1 hora)

jwt.expiration=3600000
